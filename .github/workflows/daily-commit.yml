name: Daily Auto Commit

on:
  schedule:
    - cron: '13 4 * * *'   # Runs daily at 04:13 UTC (specific time)
  workflow_dispatch:

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f2004163@ds.study.iitm.ac.in Scheduled Step
        run: |
          echo "Updated on $(date)" >> daily_log.txt

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "24f2004163@ds.study.iitm.ac.in"
          git add daily_log.txt
          git commit -m "Automated daily commit on $(date)" || echo "No changes"
          git push
